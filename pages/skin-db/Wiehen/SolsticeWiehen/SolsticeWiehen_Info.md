# Solstice Wiehen - notes

Canvas **9262 x 3102** (`canvasSize` in `manifest.json`). **Draw order (bottom -> top):** colors 1-4 -> pattern (color 5 tint) -> color 6 -> details -> lineart (multiply) -> background -> **palette** PNG -> **info** PNG (soft-light). Sidebar text is optional `infoNotes` `.md` only.
