# Pastel Sin - notes

Canvas **8000 x 3500** (`canvasSize` in `manifest.json`). Draw order (bottom -> top): colors 1-4 -> pattern (color 5 tint) -> lineart (multiply) -> background -> palette PNG -> info PNG (soft-light).

Color 6 hex is tracked for design planning, but no dedicated color 6 mask PNG is currently present in this skin pack.
