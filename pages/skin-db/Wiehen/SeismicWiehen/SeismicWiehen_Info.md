# Seismic Wiehen - notes

Canvas **9262 x 3102** (`canvasSize` in `manifest.json`). Draw order (bottom -> top): colors 1-4 -> pattern (color 5 tint) -> details -> lineart (multiply) -> background -> info PNG (soft-light).

This skin intentionally reuses Solstice details:
- `../SolsticeWiehen/SolsticeWiehen_Details.png`
